{
  "name": "pichacky",
  "version": "1.0.0",
  "description": "Time tracking application",
  "type": "module",
  "scripts": {
    "dev": "echo 'No dev server needed for static HTML'",
    "build": "echo 'No build needed for static HTML'"
  },
  "dependencies": {
    "@vercel/speed-insights": "^1.1.0"
  }
}
